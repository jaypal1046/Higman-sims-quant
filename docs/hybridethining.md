# Historical Note

This file previously contained an overconfident summary of early hybrid benchmark results.

Current guidance:

- Treat the `v1` to `v7` scripts as research iterations, not production-ready benchmark claims.
- Use the top-level `README.md` for the current project status.
- Use `src/higman_sims_quant_v7.py` for the latest implementation details.
- Do not interpret older notes in this repository as evidence that the project has beaten Google TurboQuant.

The main takeaway from the current work is that the project shows promising quality-first quantization ideas, but fair comparison still depends on honest bitrate accounting and stronger downstream benchmarks.
